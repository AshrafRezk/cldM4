{
  "name": "cloudiator-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudiator admin dashboard. No inference. No login form. Cloudflare Access is the authenticator.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:no-neon": "node scripts/check-no-neon.mjs",
    "test": "npm run build && npm run check:no-neon && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "hash-wasm": "^4.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
